{{- if eq .Values.database.type "internal" -}}
{{- if or .Values.database.internal.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- if .Values.database.internal.serviceAccount.annotations }}
  annotations: {{ toYaml .Values.database.internal.serviceAccount.annotations | nindent 4 }}
  {{- end }}
  labels:
    app: {{ template "harbor.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "harbor.database-serviceAccountName" . }}
{{- end -}}
{{- end -}}
