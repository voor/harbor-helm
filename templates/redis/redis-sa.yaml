{{- if or .Values.redis.internal.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  {{- if .Values.redis.internal.serviceAccount.annotations }}
  annotations: {{ toYaml .Values.redis.internal.serviceAccount.annotations | nindent 4 }}
  {{- end }}
  labels:
    app: {{ template "harbor.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "harbor.redis-serviceAccountName" . }}
{{- end -}}
